# Documentation

Published docs live here. Contract documents live in `../contracts/`.

- [`waiveo-derive.md`](waiveo-derive.md) — the OFF-APPLIANCE rasterizer: how a
  gradient, a drop shadow, a rounded border, a custom face or a QR code reaches a
  screen that cannot draw one, and why the box never runs a browser.
