{
  "case_id": "SUR-045-invalid-cookie-as-credential-rejected",
  "contract": "surface/1",
  "req_ids": ["SUR-045", "SUR-061"],
  "description": "Under same-host port isolation (mechanism b), a management-API request originating from the isolation origin and carrying a valid platform session cookie -- but no surface_token -- MUST be rejected: the cookie is port-blind under SameSite and MUST NOT be accepted as the surface's credential under either isolation mechanism.",
  "input": {
    "deployment_isolation_mechanism": "same-host-port",
    "request": {
      "origin": "https://box.local:8443",
      "path": "/api/v1/packs/waiveo~slidecast/actions/save",
      "headers": {
        "Cookie": "waiveo_session=s%3Avalid-session-id.signature",
        "Authorization": null
      }
    },
    "cookie_session_valid_for_host_page_origin": true
  },
  "expected": {
    "authenticated": false,
    "cookie_honored": false,
    "error": { "code": "UNAUTHENTICATED" },
    "reason": "no surface_token bearer credential was presented; the accompanying platform session cookie is never accepted as the surface's own credential"
  }
}
